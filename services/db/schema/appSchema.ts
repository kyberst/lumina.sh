
export const createAppTables = async (db: any) => {
    // No strict schema needed, but we can define standard fields if we want strict typing.
    // For now, implicit creation works best for embedded agility.
};